@model IEnumerable<BookingApp.Models.TipSmještaja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipSmještaja1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipSmještaja1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipSmještajaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipSmještajaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipSmještajaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
